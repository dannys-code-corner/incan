name: CI

on:
  push:
    branches: [ main, release/** ]
  pull_request:
    branches: [ main, release/** ]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  INCAN_NO_BANNER: 1

jobs:
  changes:
    name: CI Scope
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.scope.outputs.docs_only }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
      - name: Classify the pull request
        id: scope
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          set -euo pipefail

          docs_only=false
          if [ "$EVENT_NAME" = "pull_request" ]; then
            git fetch origin "$BASE_REF" --depth=1
            base_ref="origin/$BASE_REF"
            changed=false
            docs_only=true

            while IFS= read -r -d '' file; do
              changed=true
              # The two generated reference pages are derived from the compiler,
              # so they must retain the full generated-reference check.
              case "$file" in
                workspaces/docs-site/docs/language/reference/language.md|workspaces/docs-site/docs/language/reference/feature_inventory.md)
                  docs_only=false
                  break
                  ;;
                workspaces/docs-site/*)
                  ;;
                *)
                  docs_only=false
                  break
                  ;;
              esac
            done < <(git diff --name-only -z "$base_ref" HEAD)

            if [ "$changed" = false ]; then
              docs_only=false
            fi
          fi

          echo "docs_only=$docs_only" >> "$GITHUB_OUTPUT"

  fmt:
    name: Format Check
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      # Comment wrapping (comment_width/wrap_comments/format_code_in_doc_comments) requires nightly rustfmt.
      # We intentionally use nightly for *formatting only*; all builds/tests remain on stable/MSRV jobs.
      # Install stable first (rustup), then nightly with retries — nightly channel sync sometimes flakes on the network.
      - uses: dtolnay/rust-toolchain@stable
      - uses: ./.github/actions/install-nightly-toolchain
        with:
          components: rustfmt
      - name: Check formatting
        run: cargo +nightly fmt --all -- --check

  rustdoc-gate:
    name: Rustdoc Gate
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Check changed Rust docs
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch origin "${{ github.base_ref }}" --depth=1
            base_ref="origin/${{ github.base_ref }}"
          elif [ -n "${{ github.event.before }}" ] && [ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]; then
            base_ref="${{ github.event.before }}"
          else
            base_ref="HEAD^"
          fi
          python3 scripts/check_changed_rustdocs.py --base "$base_ref"

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          # Share registry/target across stable jobs; the Linux test shard is the cache writer.
          shared-key: incan-stable
          save-if: false
      - uses: ./.github/actions/setup-rust-sccache
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Show sccache stats
        if: always()
        run: ${SCCACHE_PATH:-sccache} --show-stats || true

  test-linux-bootstrap:
    name: Oven Suite Bootstrap (ubuntu-latest, stable)
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    env:
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v2-stable-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-stable-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Build compiler binaries for the Oven publisher
        # Bootstrap Cargo is permitted only to build the compiler executable. Test compilation and execution below
        # are receipt-bound Oven work, not a Cargo/Nextest harness.
        run: |
          CARGO_BUILD_JOBS=2 cargo build --features lsp --bins
          CARGO_BUILD_JOBS=2 cargo build -p incan_core --bin generate_lang_reference
      - name: Upload matching Incan CLI
        uses: actions/upload-artifact@v6
        with:
          name: test-linux-incan-cli
          path: target/debug/incan
          if-no-files-found: error
          retention-days: 1
      - name: Upload checked-reference generators
        uses: actions/upload-artifact@v6
        with:
          name: test-linux-reference-generators
          path: |
            target/debug/generate_lang_reference
            target/debug/generate_feature_inventory
          if-no-files-found: error
          retention-days: 1

  test-linux-provider:
    name: Oven Provider + Compiler Suite (ubuntu-latest, stable)
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-linux-bootstrap
      - smoke-release
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v3-stable-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock', 'tests/fixtures/oven_native_seed_dependencies/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-stable-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Fetch locked Oven native publisher sources
        # This publisher-only registry inventory makes the provider job's later offline native-unit publication
        # reproducible; ordinary Oven consumers never fetch or invoke Cargo.
        run: make fetch-oven-native-unit-sources
      - name: Download matching Incan CLI
        uses: actions/download-artifact@v6
        with:
          name: test-linux-incan-cli
          path: target/debug
      - name: Restore CLI executable permission
        run: chmod +x target/debug/incan
      - uses: ./.github/actions/restore-sdk-provider-store
      - name: Prewarm compiled SDK providers
        # Ordinary test shards share the immutable SDK inventory and compiler-owned native units. The separate
        # cold-provider acceptance job does not download either artifact, so it still proves first publication from
        # an empty provider store rather than inheriting a warmed test fixture.
        run: >-
          CARGO_BUILD_JOBS=2
          CARGO_NET_OFFLINE=true
          INCAN_TEST_COMPILER_ALREADY_BUILT=1
          INCAN_SOURCE_ROOT="$GITHUB_WORKSPACE"
          INCAN_STDLIB="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_STDLIB_DIR="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_TOOLCHAIN_CRATES_DIR="$GITHUB_WORKSPACE/crates"
          INCAN_GENERATED_CARGO_TARGET_DIR="$GITHUB_WORKSPACE/target/incan_generated_shared_target"
          INCAN_INTERNAL_SDK_PROVIDER_STORE="$GITHUB_WORKSPACE/target/incan_test_sdk_provider_store"
          make test-prewarm-oven-native-units
      - uses: ./.github/actions/install-nightly-toolchain
      - name: Run bounded Oven compiler suite beside its prepared closure
        # The suite consumes the same locally prepared provider/native-unit closure. Sending roughly 900 MiB through
        # the artifact service only to download it into another identical runner made the CI critical path transfer-
        # bound and does not exercise a normal Oven consumer any better.
        uses: ./.github/actions/run-oven-compiler-suite
        with:
          lane: ubuntu-latest-stable
      - name: Upload Linux compiler-suite evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v6
        with:
          name: oven-compiler-suite-linux-evidence
          path: target/oven-compiler-suite-output
          if-no-files-found: warn
          retention-days: 14
      - name: Download release compiler for normal Oven smoke commands
        uses: actions/download-artifact@v6
        with:
          name: incan-release-bin
          path: target/release
      - name: Restore release compiler executable permission
        run: chmod +x target/release/incan
      - name: Record guarded release Oven Alpha build benchmark
        # This is a small representative normal command, not the compiler-suite timing.  It uses the same release
        # compiler and locally prepared native-unit layout as the smoke set, starts with an empty Oven home, and
        # persists the Linux reference report without transferring the large test-native-unit closure.
        run: |
          clean_worktree="$RUNNER_TEMP/incan-oven-alpha-clean-worktree"
          git worktree add --detach "$clean_worktree" "$GITHUB_SHA"
          trap 'git worktree remove --force "$clean_worktree"' EXIT
          mkdir -p target/ci-benchmark-cargo-guard
          cat > target/ci-benchmark-cargo-guard/cargo <<'EOF'
          #!/usr/bin/env bash
          exit 97
          EOF
          chmod +x target/ci-benchmark-cargo-guard/cargo
          bash scripts/bench_oven_alpha.sh \
            --incan "$GITHUB_WORKSPACE/target/release/incan" \
            --release-identity "GitHub Actions incan-release-bin artifact for $GITHUB_SHA" \
            --checkout-revision "$GITHUB_SHA" \
            --workload build \
            --source "$GITHUB_WORKSPACE/tests/fixtures/oven_alpha_release_core.incn" \
            --clean-worktree-source "$clean_worktree/tests/fixtures/oven_alpha_release_core.incn" \
            --incan-home "$RUNNER_TEMP/incan-oven-alpha-benchmark-home" \
            --output "$GITHUB_WORKSPACE/target/oven-alpha-linux-benchmark" \
            --cargo-guard-dir "$GITHUB_WORKSPACE/target/ci-benchmark-cargo-guard" \
            --repetitions 2
          jq . target/oven-alpha-linux-benchmark/report.json
      - name: Upload Linux Oven Alpha benchmark evidence
        uses: actions/upload-artifact@v6
        with:
          name: oven-alpha-linux-benchmark
          path: target/oven-alpha-linux-benchmark
          if-no-files-found: error
          retention-days: 14
      - name: Run normal Oven smoke set without Cargo
        run: |
          mkdir -p target/ci-cargo-guard
          cat > target/ci-cargo-guard/cargo <<'EOF'
          #!/usr/bin/env bash
          printf '%s\n' "unexpected Cargo invocation during normal Oven smoke command: $*" >> "$CARGO_GUARD_LOG"
          exit 97
          EOF
          chmod +x target/ci-cargo-guard/cargo
          : > target/ci-cargo-guard/invocations.log
          export PATH="$GITHUB_WORKSPACE/target/ci-cargo-guard:$PATH"
          export CARGO_GUARD_LOG="$GITHUB_WORKSPACE/target/ci-cargo-guard/invocations.log"
          for make_target in \
            smoke-test-canary \
            smoke-test-web-example \
            smoke-test-nested-project-example \
            smoke-test-examples \
            smoke-test-benchmarks-incan
          do
            INCAN_EXAMPLES_TIMEOUT=20 make "$make_target"
          done
          if [ -s "$CARGO_GUARD_LOG" ]; then
            cat "$CARGO_GUARD_LOG" >&2
            exit 1
          fi
      - name: Upload compiled SDK provider store
        # The provider store is a bounded immutable product of the matching compiler, not part of the test archive.
        uses: actions/upload-artifact@v6
        with:
          name: test-linux-sdk-provider-store
          path: target/incan_test_sdk_provider_store
          if-no-files-found: error
          retention-days: 1
      - name: Upload selected SDK provider inventory
        # The sealed inventory path selects one digest-verified entry inside the immutable provider store.
        # Downstream normal-command consumers must receive this selection instead of rediscovering a provider.
        uses: actions/upload-artifact@v6
        with:
          name: test-linux-sdk-provider-path
          path: target/incan_test_sdk_provider_path
          if-no-files-found: error
          retention-days: 1

  test-linux-oven:
    name: Oven Compiler Suite (ubuntu-latest, stable)
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-linux-provider
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify provider-local bounded Oven compiler suite
        run: |
          if [ "${{ needs.test-linux-provider.result }}" != "success" ]; then
            echo "test-linux-provider finished with ${{ needs.test-linux-provider.result }}"
            exit 1
          fi
          echo "provider-local bounded Ubuntu Oven compiler suite passed"

  test-linux:
    name: Test Suite (ubuntu-latest, stable)
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-linux-oven
    if: ${{ always() && needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify bounded Oven compiler suite
        run: |
          if [ "${{ needs.test-linux-oven.result }}" != "success" ]; then
            echo "test-linux-oven finished with ${{ needs.test-linux-oven.result }}"
            exit 1
          fi
          echo "bounded Ubuntu Oven compiler suite passed"
  test-macos-bootstrap:
    name: Oven Suite Bootstrap (macos-latest, stable)
    runs-on: macos-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v3-stable-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock', 'tests/fixtures/oven_native_seed_dependencies/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-stable-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Build compiler binaries for the Oven publisher
        # Bootstrap Cargo is permitted only to build the compiler executable. Test compilation and execution below
        # are receipt-bound Oven work, not a Cargo/Nextest harness.
        run: CARGO_BUILD_JOBS=2 cargo build --features lsp --bins
      - name: Upload matching Incan CLI
        uses: actions/upload-artifact@v6
        with:
          name: test-macos-incan-cli
          path: target/debug/incan
          if-no-files-found: error
          retention-days: 1

  test-macos-provider:
    name: Oven Provider + Compiler Suite (macos-latest, stable)
    runs-on: macos-latest
    needs:
      - changes
      - test-macos-bootstrap
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v3-stable-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock', 'tests/fixtures/oven_native_seed_dependencies/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-stable-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Fetch locked Oven native publisher sources
        run: make fetch-oven-native-unit-sources
      - name: Download matching Incan CLI
        uses: actions/download-artifact@v6
        with:
          name: test-macos-incan-cli
          path: target/debug
      - name: Restore CLI executable permission
        run: chmod +x target/debug/incan
      - uses: ./.github/actions/restore-sdk-provider-store
      - name: Prewarm compiled SDK providers
        run: >-
          CARGO_BUILD_JOBS=2
          CARGO_NET_OFFLINE=true
          INCAN_TEST_COMPILER_ALREADY_BUILT=1
          INCAN_SOURCE_ROOT="$GITHUB_WORKSPACE"
          INCAN_STDLIB="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_STDLIB_DIR="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_TOOLCHAIN_CRATES_DIR="$GITHUB_WORKSPACE/crates"
          INCAN_GENERATED_CARGO_TARGET_DIR="$GITHUB_WORKSPACE/target/incan_generated_shared_target"
          INCAN_INTERNAL_SDK_PROVIDER_STORE="$GITHUB_WORKSPACE/target/incan_test_sdk_provider_store"
          make test-prewarm-oven-native-units
      - uses: ./.github/actions/install-nightly-toolchain
      - name: Run bounded Oven compiler suite beside its prepared closure
        # Keep the prepared native units local to the consumer. The direct-Rustc suite needs their immutable
        # contents, not an artifact-service round trip that can dominate the macOS critical path.
        uses: ./.github/actions/run-oven-compiler-suite
        with:
          lane: macos-latest-stable
      - name: Upload macOS compiler-suite evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v6
        with:
          name: oven-compiler-suite-macos-evidence
          path: target/oven-compiler-suite-output
          if-no-files-found: warn
          retention-days: 14
      - name: Record guarded macOS Oven Alpha build benchmark
        # The stable test-CLI artifact is identified explicitly in the report. The measured normal command uses Oven;
        # Cargo only built the compiler in its separate bootstrap job.
        run: |
          clean_worktree="$RUNNER_TEMP/incan-oven-alpha-clean-worktree"
          git worktree add --detach "$clean_worktree" "$GITHUB_SHA"
          trap 'git worktree remove --force "$clean_worktree"' EXIT
          mkdir -p target/ci-benchmark-cargo-guard
          cat > target/ci-benchmark-cargo-guard/cargo <<'EOF'
          #!/usr/bin/env bash
          exit 97
          EOF
          chmod +x target/ci-benchmark-cargo-guard/cargo
          bash scripts/bench_oven_alpha.sh \
            --incan "$GITHUB_WORKSPACE/target/debug/incan" \
            --release-identity "GitHub Actions test-macos-incan-cli artifact for $GITHUB_SHA" \
            --checkout-revision "$GITHUB_SHA" \
            --workload build \
            --source "$GITHUB_WORKSPACE/tests/fixtures/oven_alpha_release_core.incn" \
            --clean-worktree-source "$clean_worktree/tests/fixtures/oven_alpha_release_core.incn" \
            --incan-home "$RUNNER_TEMP/incan-oven-alpha-benchmark-home" \
            --output "$GITHUB_WORKSPACE/target/oven-alpha-macos-benchmark" \
            --cargo-guard-dir "$GITHUB_WORKSPACE/target/ci-benchmark-cargo-guard" \
            --repetitions 2
          jq . target/oven-alpha-macos-benchmark/report.json
      - name: Upload macOS Oven Alpha benchmark evidence
        uses: actions/upload-artifact@v6
        with:
          name: oven-alpha-macos-benchmark
          path: target/oven-alpha-macos-benchmark
          if-no-files-found: error
          retention-days: 14
      - name: Upload compiled SDK provider store
        uses: actions/upload-artifact@v6
        with:
          name: test-macos-sdk-provider-store
          path: target/incan_test_sdk_provider_store
          if-no-files-found: error
          retention-days: 1
      - name: Upload selected SDK provider inventory
        uses: actions/upload-artifact@v6
        with:
          name: test-macos-sdk-provider-path
          path: target/incan_test_sdk_provider_path
          if-no-files-found: error
          retention-days: 1

  test-macos-oven:
    name: Oven Compiler Suite (macos-latest, stable)
    runs-on: macos-latest
    needs:
      - changes
      - test-macos-provider
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify provider-local bounded Oven compiler suite
        run: |
          if [ "${{ needs.test-macos-provider.result }}" != "success" ]; then
            echo "test-macos-provider finished with ${{ needs.test-macos-provider.result }}"
            exit 1
          fi
          echo "provider-local bounded macOS Oven compiler suite passed"

  test-macos:
    name: Test Suite (macos-latest, stable)
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-macos-oven
    if: ${{ always() && needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify bounded Oven compiler suite
        run: |
          if [ "${{ needs.test-macos-oven.result }}" != "success" ]; then
            echo "test-macos-oven finished with ${{ needs.test-macos-oven.result }}"
            exit 1
          fi
          echo "bounded macOS Oven compiler suite passed"
  msrv-build:
    name: MSRV Build (1.93.0)
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust 1.93.0
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.93.0
          components: clippy
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: ./.github/actions/setup-rust-sccache
      - name: Cargo build (locked)
        run: cargo build --locked
      - name: Show sccache stats
        if: always()
        run: ${SCCACHE_PATH:-sccache} --show-stats || true

  msrv-test-bootstrap:
    name: MSRV Oven Suite Bootstrap (1.93.0)
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust 1.93.0
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.93.0
          components: clippy
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v3-msrv-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock', 'tests/fixtures/oven_native_seed_dependencies/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-msrv-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Build compiler binaries for the Oven publisher
        # Bootstrap Cargo is permitted only to build the compiler executable. Test compilation and execution below
        # are receipt-bound Oven work, not a Cargo/Nextest harness.
        run: CARGO_BUILD_JOBS=2 cargo build --locked --features lsp --bins
      - name: Upload matching Incan CLI
        uses: actions/upload-artifact@v6
        with:
          name: msrv-test-incan-cli
          path: target/debug/incan
          if-no-files-found: error
          retention-days: 1

  msrv-test-provider:
    name: MSRV Oven Provider + Compiler Suite (1.93.0)
    runs-on: ubuntu-latest
    needs:
      - changes
      - msrv-test-bootstrap
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust 1.93.0
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.93.0
          components: clippy
      - name: Install WASI target for vocab desugarers
        run: rustup target add wasm32-wasip1
      - name: Restore Cargo sources
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v3-msrv-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock', 'tests/fixtures/oven_native_seed_dependencies/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-msrv-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Fetch locked Oven native publisher sources
        run: make fetch-oven-native-unit-sources
      - name: Download matching Incan CLI
        uses: actions/download-artifact@v6
        with:
          name: msrv-test-incan-cli
          path: target/debug
      - name: Restore CLI executable permission
        run: chmod +x target/debug/incan
      - uses: ./.github/actions/restore-sdk-provider-store
      - name: Prewarm compiled SDK providers
        run: >-
          CARGO_BUILD_JOBS=2
          CARGO_NET_OFFLINE=true
          INCAN_TEST_COMPILER_ALREADY_BUILT=1
          INCAN_SOURCE_ROOT="$GITHUB_WORKSPACE"
          INCAN_STDLIB="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_STDLIB_DIR="$GITHUB_WORKSPACE/crates/incan_stdlib/stdlib"
          INCAN_TOOLCHAIN_CRATES_DIR="$GITHUB_WORKSPACE/crates"
          INCAN_GENERATED_CARGO_TARGET_DIR="$GITHUB_WORKSPACE/target/incan_generated_shared_target"
          INCAN_INTERNAL_SDK_PROVIDER_STORE="$GITHUB_WORKSPACE/target/incan_test_sdk_provider_store"
          make test-prewarm-oven-native-units
      - uses: ./.github/actions/install-nightly-toolchain
      - name: Run bounded Oven compiler suite beside its prepared closure
        # The MSRV direct-Rustc runner consumes the just-published immutable closure in this job. Keeping it local
        # avoids making a large artifact upload/download the gate for the actual all-root execution.
        uses: ./.github/actions/run-oven-compiler-suite
        with:
          lane: ubuntu-latest-msrv-1-93-0
      - name: Upload MSRV compiler-suite evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v6
        with:
          name: oven-compiler-suite-msrv-evidence
          path: target/oven-compiler-suite-output
          if-no-files-found: warn
          retention-days: 14
      - name: Upload compiled SDK provider store
        uses: actions/upload-artifact@v6
        with:
          name: msrv-test-sdk-provider-store
          path: target/incan_test_sdk_provider_store
          if-no-files-found: error
          retention-days: 1
      - name: Upload selected SDK provider inventory
        uses: actions/upload-artifact@v6
        with:
          name: msrv-test-sdk-provider-path
          path: target/incan_test_sdk_provider_path
          if-no-files-found: error
          retention-days: 1

  msrv-test-oven:
    name: MSRV Oven Compiler Suite (1.93.0)
    runs-on: ubuntu-latest
    needs:
      - changes
      - msrv-test-provider
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify provider-local bounded Oven compiler suite
        run: |
          if [ "${{ needs.msrv-test-provider.result }}" != "success" ]; then
            echo "msrv-test-provider finished with ${{ needs.msrv-test-provider.result }}"
            exit 1
          fi
          echo "provider-local bounded MSRV Oven compiler suite passed"

  msrv:
    name: MSRV Build/Test (1.93.0)
    runs-on: ubuntu-latest
    needs:
      - changes
      - msrv-build
      - msrv-test-oven
    if: ${{ always() && needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify MSRV build and bounded Oven compiler suite
        run: |
          if [ "${{ needs.msrv-build.result }}" != "success" ]; then
            echo "msrv-build finished with ${{ needs.msrv-build.result }}"
            exit 1
          fi
          if [ "${{ needs.msrv-test-oven.result }}" != "success" ]; then
            echo "msrv-test-oven finished with ${{ needs.msrv-test-oven.result }}"
            exit 1
          fi
          echo "MSRV build and bounded Oven compiler suite passed"
  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Run cargo audit
        run: cargo audit

  deny:
    name: Cargo Deny
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Install cargo-deny
        run: cargo install cargo-deny --locked
      - name: Run cargo deny
        run: cargo deny check

  udeps:
    name: Unused Dependencies
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: ./.github/actions/install-nightly-toolchain
      - name: Compute udeps cache key
        id: udeps-cache-key
        shell: bash
        run: |
          set -euo pipefail

          workspace_version="$(
            awk '
              /^\[workspace.package\]/ { in_section=1; next }
              /^\[/ { in_section=0 }
              in_section && /^version = / {
                gsub(/"/, "", $3)
                print $3
                exit
              }
            ' Cargo.toml
          )"
          if [ -z "$workspace_version" ]; then
            echo "failed to read [workspace.package].version from Cargo.toml" >&2
            exit 1
          fi

          release_line="$(
            printf '%s\n' "$workspace_version" |
              sed -E 's/^([0-9]+)\.([0-9]+)\..*$/v\1.\2/'
          )"
          if [ "$release_line" = "$workspace_version" ]; then
            echo "failed to derive release line from workspace version: $workspace_version" >&2
            exit 1
          fi

          key="$(
            {
              rustc +nightly -Vv
              cargo +nightly -V
              printf 'workspace-release-line=%s\n' "$release_line"
              git ls-files 'Cargo.toml' '*/Cargo.toml' 'Cargo.lock' '*/Cargo.lock' | sort | while read -r file; do
                printf '== %s ==\n' "$file"
                WORKSPACE_VERSION="$workspace_version" RELEASE_LINE="$release_line" perl -pe '
                  BEGIN {
                    $from = quotemeta($ENV{"WORKSPACE_VERSION"});
                    $to = $ENV{"RELEASE_LINE"};
                  }
                  s/$from/$to/g;
                ' "$file"
              done
            } | shasum -a 256 | cut -d ' ' -f 1
          )"

          echo "release-line=$release_line" >> "$GITHUB_OUTPUT"
          echo "key=$key" >> "$GITHUB_OUTPUT"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: incan-udeps
          key: ${{ steps.udeps-cache-key.outputs.key }}
          add-rust-environment-hash-key: "false"
          cache-all-crates: "true"
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: ./.github/actions/setup-rust-sccache
      - name: Install cargo-udeps
        run: cargo install cargo-udeps --locked
      - name: Run cargo udeps
        run: cargo +nightly udeps --all-targets
      - name: Show sccache stats
        if: always()
        run: ${SCCACHE_PATH:-sccache} --show-stats || true

  smoke-release:
    name: Smoke Release Build
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          # Keep release-profile artifacts separate from the dev/test cache.
          shared-key: incan-stable-release
          cache-workspace-crates: "true"
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Build release compiler once
        run: make smoke-test-release
      - name: Upload release compiler
        uses: actions/upload-artifact@v6
        with:
          name: incan-release-bin
          path: target/release/incan
          if-no-files-found: error
          retention-days: 1

  smoke-test-shards:
    name: Oven Normal-Command Smoke Set
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-linux-provider
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify provider-local normal Oven smoke set
        run: |
          if [ "${{ needs.test-linux-provider.result }}" != "success" ]; then
            echo "test-linux-provider finished with ${{ needs.test-linux-provider.result }}"
            exit 1
          fi
          echo "provider-local normal Oven smoke set passed"

  smoke-test:
    name: Smoke Test
    runs-on: ubuntu-latest
    needs:
      - changes
      - smoke-release
      - smoke-test-shards
    if: ${{ always() && needs.changes.outputs.docs_only != 'true' }}
    steps:
      - name: Verify smoke jobs
        run: |
          if [ "${{ needs.smoke-release.result }}" != "success" ]; then
            echo "smoke-release finished with ${{ needs.smoke-release.result }}"
            exit 1
          fi
          if [ "${{ needs.smoke-test-shards.result }}" != "success" ]; then
            echo "smoke-test-shards finished with ${{ needs.smoke-test-shards.result }}"
            exit 1
          fi
          echo "smoke jobs passed"

  generate-rust-docs:
    name: Docs Build
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: ./.github/actions/setup-rust-sccache
      - name: Build docs
        run: cargo doc --no-deps
      - name: Show sccache stats
        if: always()
        run: ${SCCACHE_PATH:-sccache} --show-stats || true

  docs-site:
    name: Docs Site Build (MkDocs)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workspaces/docs-site
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install MkDocs dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-docs.txt
      - name: Build docs site (strict)
        run: python -m mkdocs build --strict

  generated-reference:
    name: Generated Reference Up-to-date
    runs-on: ubuntu-latest
    needs:
      - changes
      - test-linux-bootstrap
      - test-linux-provider
    if: ${{ needs.changes.outputs.docs_only != 'true' }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - name: Restore Cargo sources
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: incan-test-cargo-sources-v2-stable-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'crates/incan_stdlib/stdlib/components/**/vocab_companion/Cargo.lock') }}
          restore-keys: |
            incan-test-cargo-sources-v1-stable-${{ runner.os }}-${{ runner.arch }}-
      - name: Fetch locked Cargo sources
        run: make fetch-locked-cargo-sources
      - name: Download matching Incan CLI
        uses: actions/download-artifact@v6
        with:
          name: test-linux-incan-cli
          path: target/debug
      - name: Download checked-reference generators
        uses: actions/download-artifact@v6
        with:
          name: test-linux-reference-generators
          path: target/debug
      - name: Restore executable permissions
        run: chmod +x target/debug/incan target/debug/generate_lang_reference target/debug/generate_feature_inventory
      - name: Fingerprint checked feature inventory
        id: inventory-identity
        run: |
          {
            echo "compiler=$(shasum -a 256 target/debug/incan | awk '{print $1}')"
            echo "lock=$(shasum -a 256 Cargo.lock | awk '{print $1}')"
            echo "stdlib=$(git rev-parse HEAD:crates/incan_stdlib/stdlib)"
          } >> "$GITHUB_OUTPUT"
      - name: Restore checked feature inventory providers
        id: inventory-provider-cache
        uses: actions/cache@v4
        with:
          path: target/incan_reference_sdk_provider_store
          key: >-
            incan-feature-inventory-provider-v1-${{ runner.os }}-${{ runner.arch }}-${{
            steps.inventory-identity.outputs.compiler }}-${{ steps.inventory-identity.outputs.lock }}-${{
            steps.inventory-identity.outputs.stdlib }}
      - name: Seed checked feature inventory providers
        if: steps.inventory-provider-cache.outputs.cache-hit != 'true'
        uses: actions/download-artifact@v6
        with:
          name: test-linux-sdk-provider-store
          path: target/incan_reference_sdk_provider_store
      - name: Regenerate checked language references
        run: ./target/debug/generate_lang_reference
      - name: Regenerate checked capability inventory
        run: ./target/debug/generate_feature_inventory
        env:
          CARGO_BUILD_JOBS: "2"
          CARGO_BIN_EXE_incan: ${{ github.workspace }}/target/debug/incan
          INCAN_SOURCE_ROOT: ${{ github.workspace }}
          INCAN_STDLIB: ${{ github.workspace }}/crates/incan_stdlib/stdlib
          INCAN_STDLIB_DIR: ${{ github.workspace }}/crates/incan_stdlib/stdlib
          INCAN_TOOLCHAIN_CRATES_DIR: ${{ github.workspace }}/crates
          INCAN_GENERATED_CARGO_TARGET_DIR: ${{ runner.temp }}/incan_reference_generated_target
          INCAN_INTERNAL_SDK_PROVIDER_STORE: ${{ github.workspace }}/target/incan_reference_sdk_provider_store
      - name: Verify no generated-reference diff
        run: git diff --exit-code workspaces/docs-site/docs/language/reference/language.md workspaces/docs-site/docs/language/reference/feature_inventory.md
