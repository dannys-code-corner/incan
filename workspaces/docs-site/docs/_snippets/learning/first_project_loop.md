```bash
incan new hello --yes
cd hello
incan run
incan test
incan build --release
```

This is the canonical first-contact loop: scaffold one project, run its entry point, execute its tests, and produce a native release build.
