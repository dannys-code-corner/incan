"""User and Product models for the shop example."""

pub model User:
    pub name: str
    pub email: str
    pub age: int = 0


pub model Product:
    pub name: str
    pub price: float
    pub quantity: int = 1


pub def product_total(product: Product) -> float:
    return product.price * float(product.quantity)
