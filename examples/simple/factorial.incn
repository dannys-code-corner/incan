"""Factorial calculation in Incan"""

def factorial(n: int) -> int:
    if n <= 1:
        return 1
    return n * factorial(n - 1)


def main() -> None:
    result = factorial(5)
    println(f"5! = {result}")
