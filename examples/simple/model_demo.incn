"""
Model and class demonstration
"""

model User:
    name: str
    age: int


def greet_user(user: User) -> str:
    return f"Hello, {user.name}!"


def main() -> None:
    println("Model demo")
    incan_user = User(name="Incan", age=42)
    println(greet_user(incan_user))
