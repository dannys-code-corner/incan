---
source: tests/codegen_snapshot_tests.rs
expression: rust_code
---
// Generated by the Incan compiler v0.1.0-alpha.1

#![allow(unused_imports, dead_code, unused_variables)]

// __INCAN_INSERT_MODS__

use incan_stdlib::prelude::*;
use incan_derive::{FieldInfo, IncanClass};
use std::collections::HashMap;
pub fn test_collections() {
    let empty_list = vec![];
    let empty_dict = HashMap::new();
    let numbers = vec![1, 2, 3, 4, 5];
    let strings = vec!["a", "b", "c"];
    let matrix = vec![vec![1, 2], vec![3, 4]];
    let simple_dict = [("a", 1), ("b", 2)].into_iter().collect::<HashMap<_, _>>();
    let int_keys = [(1, "one"), (2, "two")].into_iter().collect::<HashMap<_, _>>();
    let nested = [
        ("first", [("x", 1), ("y", 2)].into_iter().collect::<HashMap<_, _>>()),
        ("second", [("x", 3), ("y", 4)].into_iter().collect::<HashMap<_, _>>()),
    ]
        .into_iter()
        .collect::<HashMap<_, _>>();
    let pair = (1, "one");
    let triple = (1, 2, 3);
    let multiple = ("a", pair, "b", triple, 42);
    let first = numbers[(0) as usize];
    let last = numbers[numbers.len() - 1i64];
    let value = simple_dict[&"a"];
    let sub_list = numbers[(1) as usize..(3) as usize].to_vec();
    let all_but_first = numbers[(1) as usize..numbers.len()].to_vec();
    let all_but_last = numbers[0..(-1) as usize].to_vec();
}
fn main() {
    test_collections();
}
